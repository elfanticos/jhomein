const HOST_SERVER = 'localhost';

export const environment = {
  production: false,
  apiJhomein: `http://${HOST_SERVER}:3000/`,
  apiService: {
    path: {
      last_article : `article/articleLast`
    }
  }
};
